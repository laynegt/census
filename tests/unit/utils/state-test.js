import State from 'census/utils/state';
import { module, test } from 'qunit';

module('Unit | Utility | state', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = new State();
    assert.ok(result);
  });
});
